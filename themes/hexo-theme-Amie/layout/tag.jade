extends partial/layout

block site_title
  != page.tag + " · " + config.title

block description
  - var desc = page.tag + ' - ' + config.title;
  meta(name="description", content=desc)

block content
  .autopagerize_page_element: .content
    .archive.animated.fadeInDown
      ul.list-with-title
        include mixins
        +postList()
        +make_pager(__('prev'), __('next'))
